import IPageProps from "../interfaces/page";

const Dashboard: React.FunctionComponent<IPageProps> = () => {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
};

export default Dashboard;
